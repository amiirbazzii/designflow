# `@designflow/workflow-qa-review`

Owns the QA-review workflow definition, manifests, capabilities, and schemas.

It must not construct concrete runtimes or bypass approval, provenance, or
artifact validation gates. Applications import the package root only.
