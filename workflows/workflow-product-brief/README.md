# `@designflow/workflow-product-brief`

Owns the product-brief workflow definition, manifests, capability composition,
and boundary schemas.

It must not own application composition, persistence, model transport, or CLI
presentation. Use its package-root exports.
