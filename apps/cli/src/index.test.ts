// apps/cli/src/index.test.ts
import { describe, expect, test } from "bun:test";

describe("designflow CLI foundation", () => {
  test("initializes foundation", () => {
    expect(true).toBe(true);
  });
});
