[functions.ai-gateway]
# The function performs the authenticated-user validation itself. Keeping the
# platform pre-check off avoids a legacy JWT gate while preserving one
# centralized auth contract for deployed and local handler paths.
verify_jwt = false
