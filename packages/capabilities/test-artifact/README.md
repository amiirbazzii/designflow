# `@designflow/capability-test-artifact`

Owns the small deterministic capability used by end-to-end workflow tests. It
must remain test-oriented and must not become a production infrastructure
package or a source of generic shared abstractions.
