# `@designflow/workers`

Owns product-facing worker manifests and the worker registry. It provides
metadata and typed selection; it does not construct engines, model clients,
CLI output, or storage adapters.

```ts
import { createWorkerRegistry } from "@designflow/workers";
```
